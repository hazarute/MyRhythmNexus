# Example .env for license_server
# Copy to `license_server/.env` and fill values for your environment.

# App
APP_ENV=development
DEBUG=True
PROJECT_NAME=MyRhythmNexus License Server
API_V1_STR=/api/v1

# Database
# For production use a PostgreSQL URL like: postgresql://user:pass@host:5432/dbname
LICENSE_DATABASE_URL=sqlite:///./license_server.db

# Security
# Replace the SECRET_KEY with a secure random value in production
SECRET_KEY=REPLACE_ME_WITH_STRONG_SECRET
ALGORITHM=RS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Offline JWT token expiry (days)
OFFLINE_TOKEN_EXPIRE_DAYS=7

# Rate limit for license validation endpoint (slowapi format, e.g. "5/minute")
LICENSE_VALIDATE_RATE=5/minute

# RSA key file paths (dev: generated by `generate_keys.py`)
PRIVATE_KEY_PATH=license_server/private.pem
PUBLIC_KEY_PATH=license_server/public.pem

# Optional: instead of storing private key as a file in the container, you can
# set the full PEM contents as secrets in your deployment platform (recommended).
# Example (set via Railway secrets):
# LICENSE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
# LICENSE_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"
